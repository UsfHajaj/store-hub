export const environment = {
  production: false,
  /**
   * تطوير محلي: اتركها فارغة — الطلبات لـ /api تتمر عبر proxy.conf.json
   * إلى http://localhost:5035 (شغّل الـ API محلياً).
   */
  apiBaseUrl: '',
};
