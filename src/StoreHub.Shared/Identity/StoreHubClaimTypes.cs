namespace StoreHub.Shared.Identity;

public static class StoreHubClaimTypes
{
    /// <summary>Permission code claims issued at login (one claim per permission).</summary>
    public const string Permission = "permission";
}
